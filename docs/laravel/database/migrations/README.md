# Migration Baseline

Estas migrations são um baseline de implementação e devem ser revisadas contra o schema final antes de produção.

A ordem segue `docs/data-model/26-migration-order.md`.

As migrations usam UUID e MySQL.
